# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository status

This repository is currently empty (no source files, build configuration, or documentation yet). Update this file as the codebase develops with build/lint/test commands and an overview of the architecture.

## Working rules

- Always respond to the user in Korean.
- Never modify original/source data files.
- Any newly created file must have a `_new` suffix added to its filename (before the extension).
